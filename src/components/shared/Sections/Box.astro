---
import type { DivAttributes } from "@/_global/types/components";
import type { DeepGuard } from "@/_global/types/types";
import "./Box.sass";

const props: DeepGuard<DivAttributes> = Astro.props;
---
<div class:list={[ 'box', props.class ]} { ...props }>
	<slot />
</div>
